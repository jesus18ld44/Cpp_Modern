#include <iostream>
#include <string>

using namespace std;
using std::string;

int main(int argc, const char** argv) {
 
    unsigned cnt = 42;
    constexpr unsigned sz = 42;

    int array[10];
    int *parr[sz];
    
    return 0;
}